export default defineAppConfig({
  ui: {
    input: {
      slots: {
        root: 'relative flex items-center ',
      },
    },
    select: {
      slots: {
        base: [ 'relative w-full'],
      },
    },
  }
})


